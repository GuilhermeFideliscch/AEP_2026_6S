version: '3.8'
services:
  mongodb:
    image: mongo:latest
    container_name: rede-saber-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: User
    restart: always